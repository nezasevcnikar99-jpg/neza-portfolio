DATABASE_URI=postgresql://user:password@host:port/dbname
PAYLOAD_SECRET=generate-a-long-random-string

# Optional (production): only needed once media uploads should persist on Vercel.
# Create a Blob store in the Vercel dashboard (Storage tab) to get this token.
BLOB_READ_WRITE_TOKEN=
